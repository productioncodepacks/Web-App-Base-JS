/**
 * TODO: Switch out when building your application on Web App Base JS.
 * -------------------------------------------------------------------
 *
 * We have just one collection for our demo application. In your application,
 * you'll probably have multiple.. such as users, photos, etc. Create a new file
 * and schema object for each and set it in the MongoDB wrapper class.
 */


// We have just one collection for the demo app.
export const collections = {
  // buttonsCollection is what we use to reference the collection
  // throughout the code.
  // buttons is the collection name that will show in the database.
  buttonsCollection: "buttons"
}
